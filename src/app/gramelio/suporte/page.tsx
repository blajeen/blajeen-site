/**
 * A URL pública é atendida pelo HTML estático revisado do jogo via rewrite em `next.config.ts`.
 * Este arquivo mantém a cobertura de rotas do App Router sem duplicar ou reescrever o documento.
 */
export default function Page() {
  return null;
}
